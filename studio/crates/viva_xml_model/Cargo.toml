[package]
name = "viva_xml_model"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
viva-genapi-xml = { path = "../../../crates/viva-genapi-xml", default-features = false }
viva-genapi = { path = "../../../crates/viva-genapi" }

[dev-dependencies]
